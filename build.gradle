// Top-level build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Android Studio 4.x 建议的 Gradle 插件版本
        classpath "com.android.tools.build:gradle:4.2.0"  // 或 4.2.1, 4.2.2
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}